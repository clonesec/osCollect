<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <title>osCollect</title>
  <%= csrf_meta_tags %>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <!-- HTML5 shim, for IE6-8 support of HTML elements ... come on, do people really use these -->
  <!--[if lt IE 9]>
    <script src="http://html5shim.googlecode.com/svn/trunk/html5.js" type="text/javascript"></script>
  <![endif]-->
  <style>
    body {
      padding-top: 60px; /* 60px to make the container go all the way to the bottom of the topbar */
    }
  </style>
  <%= stylesheet_link_tag "application", :media => "all" %>
  <!-- <link href="images/favicon.ico" rel="shortcut icon"> -->
  <link href="images/apple-touch-icon.png" rel="apple-touch-icon">
  <link href="images/apple-touch-icon-72x72.png" rel="apple-touch-icon" sizes="72x72">
  <link href="images/apple-touch-icon-114x114.png" rel="apple-touch-icon" sizes="114x114">
  <%= javascript_include_tag "application" %>
</head>
<body>
<%= javascript_tag do %>
  window.searchResultsPageSize = 10;
  window.terms = [];
<% end %>
<%= render partial: 'layouts/navbar' %>
<% flash.each do |name, msg| %>
  <div class="alert alert-<%= name == :notice ? "success" : "error" %>">
    <%= msg  %>
  </div>
<% end %>
<div style="margin-left:100px; margin-right:100px">
<%= yield %>
</div>
<div style="clear: both"></div>
<footer style="text-align:center; margin-left:130px; margin-right:130px;">
  <hr />
  <p>2013 osCollect &nbsp;&nbsp;&nbsp;&nbsp;<%= "#{Time.now.utc.strftime("%A, %b %-d, %Y %H:%M:%S %Z")}" %></p>
</footer>
</body>
</html>